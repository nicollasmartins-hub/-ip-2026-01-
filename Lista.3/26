package main

import f "fmt"

func main() {
	soma := 0.0
	numerador := 100.0

	for i := 0; i < 20; i++ {
		fatorial := 1.0
		for j := 1; j <= i; j++ {
			fatorial *= float64(j)
		}

		soma += numerador / fatorial
		numerador--
	}

	f.Printf("Soma dos 20 primeiros termos: %.4f\n", soma)
}
