package main

import f "fmt"

func main() {
	var n1 int
	f.Print("Digite um número inteiro positivo: ")
	f.Scan(&n1)

	if num < 0 {
		f.Println("Por favor, insira apenas números positivos.")
		return
	}

	triangular := false
	for i := 1; i*(i+1)*(i+2) <= num; i++ {
		if i*(i+1)*(i+2) == num {
			f.Printf("O número %d é triangular (%d x %d x %d)\n", num, i, i+1, i+2)
			triangular = true
			break
		}
	}

	if !triangular {
		f.Printf("O número %d NÃO é triangular.\n", num)
	}
}
